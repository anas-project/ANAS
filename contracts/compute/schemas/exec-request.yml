type: object
additional_properties: false
required: [provider, interface, provider_project, instance_id, command, stdin]
properties:
  provider: {type: string}
  interface: {enum: [incus_vm]}
  provider_project: {type: string}
  instance_id: {type: string}
  command:
    type: array
    min_items: 1
    items: {type: string}
  stdin:
    enum: [required_secret_stream]
