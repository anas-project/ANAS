type: object
additional_properties: false
required: [host, port, database, username, password_secret, network]
properties:
  host: {type: string}
  port: {type: integer, minimum: 1, maximum: 65535}
  database: {type: string}
  username: {type: string}
  password_secret: {type: string}
  network: {type: string}
