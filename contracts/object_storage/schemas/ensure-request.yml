type: object
additional_properties: false
required: [consumer, resource_id, provider, interface, spec]
properties:
  consumer: {type: string}
  resource_id: {type: string}
  provider: {type: string}
  interface: {enum: [s3]}
  spec: {$ref: resource.yml}
