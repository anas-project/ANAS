branch:
  - reg: ^master$
    buttons:
      - name: 从 GHCR 同步全部 Cask 镜像
        description: 灾备恢复时把 GHCR 中缺失的固定版本镜像复制到 CNB
        event: web_trigger_mirror_all
        isDefault: true
        permissions:
          roles:
            - owner
            - master
