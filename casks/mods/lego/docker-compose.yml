services:
  anas_lego:
    build:
      context: ./lego
      network: ${DOCKER_BUILD_NETWORK:-default}
      args:
        - CHINESE_SPEEDUP
        - APK_MIRROR_URL
        - DOCKER_HUB_REGISTRY
    container_name: ${CONTAINER_PREFIX}lego
    image: ${IMAGE_PREFIX}lego:5.3.1
    restart: unless-stopped
    volumes:
      - "${LEGO_DATA_PATH}:/certs"
    env_file:
      - .env

networks:
  default:
    driver: bridge
    name: ${NETWORK_PREFIX}lego
