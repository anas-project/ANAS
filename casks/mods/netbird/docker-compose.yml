version: "3.5"
services:
  #UI dashboard
  anas_dashboard:
    build: ./dashboard
    container_name: ${CONTAINER_PREFIX}${MODULE_NAME}_dashboard
    image: ${IMAGE_PREFIX}${MODULE_NAME}_dashboard:latest
    restart: unless-stopped
    env_file:
      - .env
    labels:
      - traefik.enable=true
      - anas.traefik.instance=${NETWORK_PREFIX}traefik
      - traefik.http.routers.netbird-dashboard.rule=Host(`${NETBIRD_DOMAIN}`)
      - traefik.http.routers.netbird-dashboard.tls=true
      - traefik.http.services.netbird-dashboard.loadbalancer.server.port=80
    networks:
      - traefik

  # Signal
  anas_signal:
    image: netbirdio/signal:0.25.5
    container_name: ${CONTAINER_PREFIX}${MODULE_NAME}_signal
    restart: unless-stopped
    env_file:
      - .env
    labels:
      - traefik.enable=true
      - anas.traefik.instance=${NETWORK_PREFIX}traefik
      - traefik.http.routers.netbird-signal.rule=Host(`${NETBIRD_DOMAIN}`) && PathPrefix(`/signalexchange.SignalExchange/`)
      - traefik.http.routers.netbird-signal.tls=true
      - traefik.http.services.netbird-signal.loadbalancer.server.port=80
      - traefik.http.services.netbird-signal.loadbalancer.server.scheme=h2c
    networks:
      - traefik

  # Management
  anas_management:
    build: ./management
    container_name: ${CONTAINER_PREFIX}${MODULE_NAME}_management
    image: ${IMAGE_PREFIX}${MODULE_NAME}_management:latest
    restart: unless-stopped
    env_file:
      - .env
    labels:
      - traefik.enable=true
      - anas.traefik.instance=${NETWORK_PREFIX}traefik
      - traefik.http.routers.netbird-api.rule=Host(`${NETBIRD_DOMAIN}`) && PathPrefix(`/api`)
      - traefik.http.routers.netbird-api.service=netbird-api
      - traefik.http.routers.netbird-api.tls=true
      - traefik.http.services.netbird-api.loadbalancer.server.port=8000
      - traefik.http.services.netbird-api.loadbalancer.server.scheme=h2c
      - traefik.http.routers.netbird-management.rule=Host(`${NETBIRD_DOMAIN}`) && PathPrefix(`/management.ManagementService/`)
      - traefik.http.routers.netbird-management.service=netbird-management
      - traefik.http.routers.netbird-management.tls=true
      - traefik.http.services.netbird-management.loadbalancer.server.port=8000
      - traefik.http.services.netbird-management.loadbalancer.server.scheme=h2c
    networks:
      - traefik

networks:
  traefik:
    external: true
    name: ${NETWORK_PREFIX}traefik
